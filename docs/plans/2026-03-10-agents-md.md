# AGENTS.md Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a root `AGENTS.md` that gives coding agents concise repository-specific collaboration guidance.

**Architecture:** Keep `AGENTS.md` short and execution-oriented. Reuse `docs/AI_CONTEXT.md` as the deep reference instead of duplicating architecture details.

**Tech Stack:** Markdown, Go repository conventions

---

### Task 1: Draft repository guidance

**Files:**
- Create: `docs/plans/2026-03-10-agents-md-design.md`
- Create: `docs/plans/2026-03-10-agents-md.md`
- Create: `AGENTS.md`

**Step 1: Write the document outline**

Include:

- project snapshot
- where to change code
- working rules
- validation
- documentation sync
- gotchas

**Step 2: Write `AGENTS.md` content**

Cover:

- main entrypoints and package layout
- file targets for common tasks
- rules for registration, config changes, and i18n/doc updates
- verification commands: `go test ./...` and `make build`

**Step 3: Review for duplication**

Ensure the file links to `docs/AI_CONTEXT.md` for deeper background instead of repeating it.

**Step 4: Verify repository state**

Run:

```bash
git diff -- AGENTS.md docs/plans/2026-03-10-agents-md-design.md docs/plans/2026-03-10-agents-md.md
git status --short
```

Expected:

- only the new documentation files from this task are added
- unrelated existing changes remain untouched
